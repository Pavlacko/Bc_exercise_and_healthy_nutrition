@{
    ViewData["Title"] = "BMR kalkulačka";
}

<h2 class="page-title">BMR kalkulačka</h2>

<p class="small-help">
    BMR (Basal Metabolic Rate) predstavuje množstvo energie,
    ktoré telo spotrebuje v úplnom pokoji na udržanie základných
    životných funkcií, ako je dýchanie, činnosť orgánov a termoregulácia.
</p>

<p class="small-help">
    Hodnota BMR slúži ako východisko pre výpočet odporúčaného
    denného kalorického príjmu.
</p>

<div class="card-custom" style="max-width:400px;">
    <label class="form-label">Pohlavie</label>
    <select id="bmrSex" class="form-select mb-2">
        <option value="m">Muž</option>
        <option value="f">Žena</option>
    </select>

    <label class="form-label">Hmotnosť (kg)</label>
    <input id="bmrWeight" type="number" class="form-control mb-2" />

    <label class="form-label">Výška (cm)</label>
    <input id="bmrHeight" type="number" class="form-control mb-2" />

    <label class="form-label">Vek</label>
    <input id="bmrAge" type="number" class="form-control mb-2" />

    <button id="bmrBtn" class="btn btn-main">Vypočítať BMR</button>

    <div id="bmrResult" class="mt-3 fw-bold"></div>
</div>

@section Scripts {
    <script>
        document.getElementById("bmrBtn").addEventListener("click", () => {
            const w = Number(bmrWeight.value);
            const h = Number(bmrHeight.value);
            const a = Number(bmrAge.value);
            const s = bmrSex.value;

            if (!w || !h || !a) return;

            let bmr = s === "m"
                ? 10*w + 6.25*h - 5*a + 5
                : 10*w + 6.25*h - 5*a - 161;

            bmrResult.textContent = "BMR: " + Math.round(bmr) + " kcal/deň";
        });
    </script>
}